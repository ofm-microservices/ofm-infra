apiVersion: 1

providers:
  - name: OFM Metrics
    orgId: 1
    folder: OFM
    folderUid: ofm
    type: file
    disableDeletion: false
    allowUiUpdates: true
    updateIntervalSeconds: 30
    options:
      path: /var/lib/grafana/dashboards/provisioned
